alpha = 0.05 # two-tailed alpha-level
upper bound of rejection region
r = qbinom(c(1 - (alpha/2)), m, 0.5) - (m/2)
ps = seq(from = 0, to = 1, by = 0.01)
powers = c()
for (p in ps) {
powers <- c(powers,
sum(dbinom(((m/2)+r):m, m, p)) +
sum(dbinom(0:((m/2)-r), m, p)))
}
upper bound of rejection region
r = qbinom(c(1 - (alpha/2)), m, 0.5) - (m/2)
ps = seq(from = 0, to = 1, by = 0.01)
powers = c()
for (p in ps) {
powers <- c(powers,
sum(dbinom(((m/2)+r):m, m, p)) +
sum(dbinom(0:((m/2)-r), m, p)))
}
alpha = 0.05 # two-tailed alpha-level
ms    = c(10, 100, 1000) # sample sizes
m = 10;
# upper bound of rejection region
r = qbinom(c(1 - (alpha/2)), m, 0.5) - (m/2)
ps = seq(from = 0, to = 1, by = 0.01)
powers = c()
for (p in ps) {
powers <- c(powers,
sum(dbinom(((m/2)+r):m, m, p)) +
sum(dbinom(0:((m/2)-r), m, p)))
}
plot(power)
plot(powers)
alpha = 0.05 # two-tailed alpha-level
ms    = c(10, 100, 1000) # sample sizes
m = 10;
# upper bound of rejection region
r = qbinom(c(1 - (alpha/2)), m, 0.5) - (m/2)
ps = seq(from = 0, to = 1, by = 0.01)
powers = c()
for (p in ps) {
powers <- c(powers,
sum(dbinom(((m/2)+r):m, m, p)) +
sum(dbinom(0:((m/2)-r), m, p)))
}
lot(ps, powers, xlab = "True probability of heads",
ylab = "Power", type='l', las = 1)
alpha = 0.05 # two-tailed alpha-level
ms    = c(10, 100, 1000) # sample sizes
m = 10;
# upper bound of rejection region
r = qbinom(c(1 - (alpha/2)), m, 0.5) - (m/2)
ps = seq(from = 0, to = 1, by = 0.01)
powers = c()
for (p in ps) {
powers <- c(powers,
sum(dbinom(((m/2)+r):m, m, p)) +
sum(dbinom(0:((m/2)-r), m, p)))
}
plot(ps, powers, xlab = "True probability of heads",
ylab = "Power", type='l', las = 1)
alpha = 0.05 # two-tailed alpha-level
ms    = c(10, 100, 1000) # sample sizes
m = 1000;
# upper bound of rejection region
r = qbinom(c(1 - (alpha/2)), m, 0.5) - (m/2)
ps = seq(from = 0, to = 1, by = 0.01)
powers = c()
for (p in ps) {
powers <- c(powers,
sum(dbinom(((m/2)+r):m, m, p)) +
sum(dbinom(0:((m/2)-r), m, p)))
}
plot(ps, powers, xlab = "True probability of heads",
ylab = "Power", type='l', las = 1)
alpha = 0.05 # two-tailed alpha-level
ms    = c(10, 100, 1000) # sample sizes
m = 10;
# upper bound of rejection region
r = qbinom(c(1 - (alpha/2)), m, 0.5) - (m/2)
ps = seq(from = 0, to = 1, by = 0.01)
powers = c()
for (p in ps) {
powers <- c(powers,
sum(dbinom(((m/2)+r):m, m, p)) +
sum(dbinom(0:((m/2)-r), m, p)))
}
plot(ps, powers, xlab = "True probability of heads",
ylab = "Power", type='l', las = 1)
